# Copy to .env and adjust. Never commit .env.

APP_ENV=local
APP_DEBUG=true

# Database (defaults match docker-compose.yml)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=gomrok
DB_USER=gomrok
DB_PASSWORD=gomrok
DB_CHARSET=utf8mb4
