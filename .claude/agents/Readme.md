# .claude/agents/

Custom subagent definitions for this project (one `*.md` per agent, with YAML frontmatter for
name / description / tools / model). Empty for now — add agents when a repeatable, well-scoped
job appears (e.g. a `database-agent` that only proposes migrations, a `review-agent` for the
per-phase decision check). Claude Code auto-discovers `*.md` files here.
