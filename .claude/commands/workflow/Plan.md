---
description: Multi-agent workflow variant of /Plan. Produce an implementation plan for a task without writing code. TEMPLATE.
---

# /workflow:Plan

**Status:** template.

Multi-agent variant of `/Plan` — coordinates specialised agents from `.claude/agents/`
(e.g. Plan → planning agent → implementation agent → review/QA agents).

## Purpose

Produce an implementation plan for a task without writing code.

## Agent pipeline

1. _(placeholder — which agent runs first, what it produces)_
2. _(placeholder)_

## Notes

- Same rules as `/Plan`; this variant fans work out to subagents and re-integrates the results.
