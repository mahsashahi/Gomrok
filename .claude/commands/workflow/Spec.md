---
description: Multi-agent workflow variant of /Spec. Turn a request into a written spec (scope, acceptance criteria, open questions). TEMPLATE.
---

# /workflow:Spec

**Status:** template.

Multi-agent variant of `/Spec` — coordinates specialised agents from `.claude/agents/`
(e.g. Spec → planning agent → implementation agent → review/QA agents).

## Purpose

Turn a request into a written spec (scope, acceptance criteria, open questions).

## Agent pipeline

1. _(placeholder — which agent runs first, what it produces)_
2. _(placeholder)_

## Notes

- Same rules as `/Spec`; this variant fans work out to subagents and re-integrates the results.
