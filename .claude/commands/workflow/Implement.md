---
description: Multi-agent workflow variant of /Implement. Implement a scoped change or a phase step, with tests and real evidence. TEMPLATE.
---

# /workflow:Implement

**Status:** template.

Multi-agent variant of `/Implement` — coordinates specialised agents from `.claude/agents/`
(e.g. Implement → planning agent → implementation agent → review/QA agents).

## Purpose

Implement a scoped change or a phase step, with tests and real evidence.

## Agent pipeline

1. _(placeholder — which agent runs first, what it produces)_
2. _(placeholder)_

## Notes

- Same rules as `/Implement`; this variant fans work out to subagents and re-integrates the results.
