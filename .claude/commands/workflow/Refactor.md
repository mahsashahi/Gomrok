---
description: Multi-agent workflow variant of /Refactor. Improve existing code (structure, naming, duplication) without changing behaviour. TEMPLATE.
---

# /workflow:Refactor

**Status:** template.

Multi-agent variant of `/Refactor` — coordinates specialised agents from `.claude/agents/`
(e.g. Refactor → planning agent → implementation agent → review/QA agents).

## Purpose

Improve existing code (structure, naming, duplication) without changing behaviour.

## Agent pipeline

1. _(placeholder — which agent runs first, what it produces)_
2. _(placeholder)_

## Notes

- Same rules as `/Refactor`; this variant fans work out to subagents and re-integrates the results.
