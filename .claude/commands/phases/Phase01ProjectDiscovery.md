# Project discovery — phase command stub

**Status:** skeleton file from `.claude/struct.md`. `phase-01-project-discovery` is a generic template name, **not**
one of Gomrok's phases.

Gomrok's real plan is the **30 phases in `.claude/docs/Phases.md`**. For any phase:

- Scope / exit criteria / status → `.claude/docs/Phases.md` (Status & execution tracking table at
  the top; per-phase sections below).
- Decisions (the 5+ questions) → `.claude/PhaseResults/PhaseDecisions.md`.
- What actually happened → `.claude/PhaseResults/PhaseNNResult.md`.
- The per-phase ritual (8 steps) → `.claude/docs/Phases.md` → *How each phase runs*.

Roughly, this template phase maps to: project discovery ≈ Gomrok Phase 1 (architecture baseline).
