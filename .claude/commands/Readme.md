# .claude/commands/

Custom slash-command definitions (one `*.md` per command; the filename is the command name).
Empty for now. Candidates for later: `/phase-start`, `/phase-finish` (drive the
`.claude/docs/Phases.md` workflow), `/db-design` (the propose→confirm→migrate loop). Claude Code
auto-discovers `*.md` files here.
