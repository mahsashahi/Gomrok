# .claude/skills/

Reusable implementation guides — how-to patterns and checklists per technology or task
(one folder per skill, each with a `SKILL.md`). Empty for now. Candidates: `add-provider-adapter`
(the core port + optional interfaces + capabilities recipe), `add-module` (the per-module
Domain/Application/Infrastructure/Http layout), `write-migration` (Phinx conventions, once
Phase 4 sets them).
