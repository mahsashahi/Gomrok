# Orders.md — requirements & decisions register

**Purpose.** A single register of every requirement, decision, and business rule for Gomrok, with
date, source, status, and the related phase — the "why" behind the build.

**Relationship to other files.** The detailed phase-by-phase decision Q&A lives in
`.claude/PhaseResults/PhaseDecisions.md` (options, recommendation, selection). This file is the higher-level
index: one row per requirement/decision, pointing at where it is specified. Do not duplicate the
full text — link to `CLAUDE.md`, `.claude/Rule.md`, `.claude/docs/Architecture.md`, or
`.claude/PhaseResults/PhaseDecisions.md`.

## How to use

- Add a row whenever a requirement or decision is agreed (in conversation, a phase, or a spec).
- `Status`: proposed · accepted · implemented · superseded.
- Keep newest at the top of each section.

## Product & business requirements

| # | Requirement | Date | Source | Status | Phase | Reference |
|---|---|---|---|---|---|---|
| _(none recorded here yet — see `CLAUDE.md` for the product spec)_ | | | | | | |

## Architecture & technical decisions

| # | Decision | Date | Source | Status | Phase | Reference |
|---|---|---|---|---|---|---|
| D1 | Module-based hexagonal, per-module layers | 2026-09-06 | Phase 1 Q1 | accepted | 1 | `.claude/PhaseResults/PhaseDecisions.md` |
| D2 | Direct interface calls + in-process domain events | 2026-09-06 | Phase 1 Q2 | accepted | 1 | `.claude/PhaseResults/PhaseDecisions.md` |
| D3 | ~~BIGINT PK + public ULID~~ → **plain `INT AUTO_INCREMENT` IDs, no ULID/UUID/typed-ID classes** | 2026-09-06 → **changed 2026-09-08** | Phase 1 Q3 (user) | superseded → accepted | 1 / 3 | `.claude/PhaseResults/PhaseDecisions.md`, `Architecture.md` §6 |
| D4 | Money via brick/money wrapped in a `Money` VO | 2026-09-06 | Phase 1 Q4 | accepted | 1 | `.claude/PhaseResults/PhaseDecisions.md` |
| D5 | Provider adapters: core port + optional capability interfaces | 2026-09-06 | Phase 1 Q5 | accepted | 1 | `.claude/PhaseResults/PhaseDecisions.md` |
| D6 | Docker Compose · PHP 8.4 · Phinx · PHPUnit 11 · PHPStan max | 2026-09-06 | Phase 2 Q1–Q5 | implemented | 2 | `.claude/PhaseResults/PhaseDecisions.md` |
| D7 | `src/Bootstrap/` + `src/Http/` kept as app-level dirs | 2026-09-06 | Phase 2 Q6 | implemented | 2 | `.claude/PhaseResults/PhaseDecisions.md` |
| D8 | Idempotency = lock + entity mapping (no stored response bodies); audit = full before/after row snapshots; error log = explicit writer only; idempotency TTL 24h + purge job; migration CI via GitHub Actions | 2026-09-08 | Phase 5 Q1–Q5 | implemented | 5 | `.claude/PhaseResults/PhaseDecisions.md`, `Architecture.md` §11 |
| D9 | API key = prefixed token + `sha256(secret)` looked up by public `key_id`; client settings = typed columns + `client_endpoints` table; required immutable `slug`; soft reversible client disable (keys untouched); onboarding via CLI + `APP_ENV`-gated dev seeder | 2026-09-08 | Phase 6 Q1–Q5 | implemented | 6 | `.claude/PhaseResults/PhaseDecisions.md` |
| D10 | Auth = `Authorization: Bearer` only; authenticated client in a `ClientContext` holder + request attributes; `last_used_at` written throttled (≤1/key/5min); `401` for any credential fault + `403 client_disabled`, generic bodies; `Idempotency-Key` required on `/api/v1` writes; failed/successful auth logged to `client_auth_attempts`; **no rate limiting yet** | 2026-09-08 | Phase 7 Q1–Q5 | implemented | 7 | `.claude/PhaseResults/PhaseDecisions.md`, `Architecture.md` §11 |
| D11 | `Capability` enum (source of truth) + seeded `provider_capabilities` mirror; **separate `PurchaseType` enum** (not capability flags); per-type declarations in `provider_type_capabilities` / `provider_type_purchase_types` join tables; payment methods = code-only enum + in-code `MethodCapabilityRules`, no method tables yet; seed **stripe + paypal** only | 2026-09-09 | Phase 8 Q1–Q5 | implemented | 8 | `.claude/PhaseResults/PhaseDecisions.md`, `Architecture.md` §8 |
| D12 | Provider secret keys encrypted at rest via a `SecretCipher` port (`SodiumSecretCipher` / libsodium, key from `APP_ENCRYPTION_KEY`); `provider_accounts.mode` (live/test) matched to the API key prefix; `provider_account_endpoints` table (token + encrypted signing secret) for webhook/callback config; account carries `provider_account_countries` + `provider_account_methods` only, capabilities inherited from the type; CLI + env-gated dev seeder | 2026-09-09 | Phase 9 Q1–Q5 | implemented | 9 | `.claude/PhaseResults/PhaseDecisions.md`, `Architecture.md` §8/§11 |
| D13 | Country→provider routing = **provider groups only** (no `country_provider_configs` tables); group-level `provider_group_purchase_types` / `_methods` enablement, intersected at resolve time with each account's provider-type declaration; `ProviderRouter` returns an ordered candidate list + `RoutingDecision` snapshot VO (`toArray`/`fromArray`), **no routing table this phase** (persisted with the payment in Phase 17); Ziraat + Mollie type declarations seeded; unsupported combinations rejected, never downgraded | 2026-09-09 | Phase 10 Q1–Q5 | implemented | 10 | `.claude/PhaseResults/PhaseDecisions.md`, `Architecture.md` §8 |
| D14 | Package availability = **four dedicated join tables** (`package_countries` / `_currencies` / `_payment_methods` / `_provider_accounts`), each **fail open** (empty set = available everywhere for that dimension); lean `packages` table (`code` unique per client, `metadata` JSON — Phase 12 adds trial/badge/purchase-caps additively); `PackageCatalog::resolve()` internal port only, **`GET /api/v1/packages` deferred to Phase 13**; `SetPackageAvailability` full-replace handler + `composer package:*` CLI + env-gated seeder | 2026-09-10 | Phase 11 Q1–Q5 | implemented | 11 | `.claude/PhaseResults/PhaseDecisions.md`, `Architecture.md` §8 |
| D15 | Package purchase types = `package_purchase_capabilities` join table (per-row `has_trial` / `trial_days` / `duration_months`; **fail closed** — no rows = not sellable) + `package_country_purchase_capabilities` override that **replaces** the global set for a country; `badge` / `highlighted` / `client_package_id` as additive `packages` columns; `package_provider_definitions` (one row per `(package, provider account)`, lazily created, `sync_state` `not_created`/`synced`/`drift`/`not_needed`) — manual `remote_id` now, provider-API creation deferred to Phases 21–23; `synced → drift` swept explicitly inside the package-editing handlers (no event bus) | 2026-09-10 | Phase 12 Q1–Q5 | implemented | 12 | `.claude/PhaseResults/PhaseDecisions.md`, `Architecture.md` §8 |
| D21 | Decision snapshots (Phase 18) = new **`Checkout`** module (Q2) anchored by **`checkout_attempts`** — a fully user-specified central pre-payment table (Q1, a substantial expansion of the originally proposed options): `attempt_reference` external idempotent key, `id` the internal anchor every decision-snapshot table FKs to, reusing the Phase 17 `attempt_reference` device one layer up; **`CheckoutAttemptStatus`** = a monotonic-rank state machine dictated in full by the user (Q3, not chosen from an option letter) — 9 ranked happy-path statuses + 4 unranked exits reachable from any non-terminal status, skipping ranks allowed, `converted_to_payment` gated on `confirmed`, terminal once reached; only `started→pricing_resolved→(voucher_reserved→)provider_selected`, the no-op, and any non-terminal→exit implemented for real this phase, the rest modelled for Payments (Phase 20) / provider adapters (Phase 21+); three write-once decision-snapshot tables, one per owning module — `pricing_decision_snapshots` / `voucher_decision_snapshots` (thin, no amounts) / `provider_routing_decision_snapshots` — each `UNIQUE (checkout_attempt_id)`, no repository update method (Q4); one handler per lifecycle step + `CheckoutAttempt::commercialSnapshot()` for the future payment-conversion copy (Q5) + `checkout:*` CLI | 2026-09-11 | Phase 18 Q1–Q5 | implemented | 18 | `.claude/PhaseResults/PhaseDecisions.md`, `.claude/Voucher.md`, `Architecture.md` §3/§8/§9/§13 |
| D20 | Voucher redemption (Phase 17) = one `voucher_redemptions` table, reserve → confirm/release, identified by a caller-supplied `attempt_reference` (Q1; Phase 20 will pass the payment id); three states, `reserved` counts toward every cap immediately, **no automatic expiry** — a stale-reservation sweep is deferred to Phase 29 (Q2); concurrency safety = `SELECT ... FOR UPDATE` on the `vouchers` row in every reserve/confirm/release transaction, caps re-checked under that lock before any write (Q3); `VoucherDiscountCalculator` always clamps to `[0, price]` (configured cap, then price floor), carrying both `nominalDiscountMinor` (pre-clamp) and `appliedDiscountMinor` (post-clamp) rather than rejecting (Q4); three lifecycle handlers (`ReserveVoucherRedemption` re-runs the now usage-aware `VoucherEligibilityEvaluator` as the authoritative gate inside the lock, `ConfirmVoucherRedemption` increments `vouchers.redeemed_count` once, `ReleaseVoucherRedemption`) + `PdoVoucherRedemptionRepository` implementing the Phase 16 `VoucherUsagePort` + `voucher:reserve\|confirm\|release\|list-redemptions` CLI (Q5) | 2026-09-11 | Phase 17 Q1–Q5 | implemented | 17 | `.claude/PhaseResults/PhaseDecisions.md`, `.claude/Voucher.md`, `Architecture.md` §8/§9 |
| D19 | Vouchers (Phase 16 — definitions & eligibility) = one `voucher_eligibility_rules` table `(voucher_id, dimension, value)` across 7 dimensions, OR within/AND across, no rows = unrestricted (Q1); a **default discount** on `vouchers` (`none`/`percentage`/`full`, never `fixed`) plus optional per-currency **override** rows in `voucher_currency_discounts` (any type + cap), resolution override → default → `none`-inapplicable (Q2, user extended the recommended "amounts table" into a default+override model); usage limits as **nullable columns on `vouchers`** (`max_total_redemptions`/`max_per_user`/`max_per_client`, `NULL` = unlimited; user overrode the recommended `voucher_usage_limits` child table) + `redeemed_count` (Q3); `VoucherEligibilityEvaluator` returns **every** unmet reason, not fail-fast, checking state/window/scope/discount-applicability/min-purchase/first-purchase/global cap, with per-user/per-client deferred to Phase 17 behind a declared `VoucherUsagePort` (Q4); granular audited handlers + `voucher:*` CLI + seeder (Q5). **New standing rule:** `.claude/Voucher.md` is the source of truth for all voucher behaviour — every voucher change must also be recorded there (`CLAUDE.md` "Voucher Rules File") | 2026-09-10 | Phase 16 Q1–Q5 | implemented | 16 | `.claude/PhaseResults/PhaseDecisions.md`, `.claude/Voucher.md`, `Architecture.md` §8 |
| D18 | A/B price lists (Phase 15) = **`price_lists`** (one **explicit control** row per pricing group — `is_control`, `factor 1.0000`, undeletable, never disabled, created with the group + backfilled by migration; Q1 **Option 2**, user overrode the "implicit" recommendation) + **`price_list_packages`** (exact per-package amount overriding the list `factor`; Q2 Option 3). Resolution: `PriceListResolver` runs **after** the Phase 13 base amount and **before** the Phase 14 `price_rules` step (Q3 Option 1) — exact list-package amount → else `base × factor` (HALF_EVEN) → else base unchanged; `ResolvedPrice.source = price_list` when moved, always stamps `priceListId`/`Name`/`Factor`; an unknown / foreign-group / disabled list id falls back to control. `CreatePriceList` / `ChangePriceListStatus` / `SetPriceListFactor` / `SetPriceListPackagePrice` audited handlers + `pricing:*-list*` CLI + seeder. **Q4 (assignment persistence) + Q5 (management surface / endpoint wiring) DEFERRED by the user to Phase 24 (Payment creation flow)** — no `price_list_assignments` table, no bucketing service, no `visitor_ref` params this phase; `PriceResolver::resolve` passes `$priceListId = null` so every resolve uses the control list. Re-ask Q4/Q5 with full options at Phase 24. | 2026-09-10 | Phase 15 Q1–Q3 (Q4/Q5 deferred) | implemented (partial — assignment deferred) | 15 / 24 | `.claude/PhaseResults/PhaseDecisions.md`, `Architecture.md` §8/§9/§13 |
| D17 | Pricing dimension overrides = **one `price_rules` table** keyed by 7 nullable dimensions (`pricing_group_id`, `country_code`, `provider_account_id`, `payment_method`, `purchase_type`, `subscription_interval`, `currency_code`; null = wildcard); new `SubscriptionInterval` enum (monthly/quarterly/yearly); row-level `is_available` + `amount_minor`; dedicated `PriceRuleResolver` picks the winner by **matched-dimension count → fixed dimension priority (`subscription_interval > purchase_type > payment_method > provider_account_id > currency_code > country_code > pricing_group_id`) → highest `id`**; unavailable winner → hard `pricing.combination_unavailable`, **no fallback**; layered in `PriceResolver::applyRules` after the Phase 13 base price (`ResolvedPrice.source = dimension_override`); `PriceCatalog` browse list unaffected; `/pricing/resolve` gains `method`/`purchase_type`/`interval` query params; `SetPriceRule`/`DeletePriceRule` audited handlers + `pricing:set-rule`/`delete-rule`/`list-rules` CLI + seeder | 2026-09-10 | Phase 14 Q1–Q5 | implemented | 14 | `.claude/PhaseResults/PhaseDecisions.md`, `Architecture.md` §8/§9 |
| D16 | Pricing groups = **priority-ordered, overlapping** country membership + optional `device_type`, `is_default` pinned last (distinct from the exclusive Phase 10 provider groups); **one** `default_package_prices` baseline per package + `client_exchange_rates` (client-configured, effective-dated) for `status=default` cross-currency conversion via `Money::convertTo` (HALF_EVEN); `pricing_group_packages` per pair (`status` default/override/disabled, no row = implicit default, override currency = group currency); `PriceResolver`/`PriceCatalog` → `ResolvedPrice` (`baseline`/`converted`/`group_override`); `GET /api/v1/packages` + `GET /api/v1/pricing/resolve` mounted (GET not POST — pure reads, avoid write-idempotency); one audited handler per operation + `pricing:*` CLI + seeder | 2026-09-10 | Phase 13 Q1–Q5 | implemented | 13 | `.claude/PhaseResults/PhaseDecisions.md`, `Architecture.md` §8/§9 |

## Workflow & process rules

| # | Rule | Date | Source | Status | Reference |
|---|---|---|---|---|---|
| P1 | PascalCase file/dir naming | 2026-09-06 | user | accepted | `.claude/Rule.md` §3.1 |
| P2 | All docs under `.claude/` | 2026-09-07 | user | accepted | `.claude/Rule.md` §3.3 |
| P3 | One-at-a-time decision questions → `PhaseResults/PhaseDecisions.md` | 2026-09-06 | user | accepted | `.claude/Rule.md` §4.2 |
| P4 | Per-phase result file in `.claude/PhaseResults/` | 2026-09-06 | user | accepted | `.claude/Rule.md` §4.1 |
| P5 | `last_ai_answer.md` single-slot response log (renamed from `LastAiAnswer.md` 2026-09-09) | 2026-09-06 | user | accepted | `.claude/Rule.md` §1 |
