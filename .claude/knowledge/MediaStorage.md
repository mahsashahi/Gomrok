# MediaStorage.md

**Purpose.** Where and how Gomrok stores media / files in real environments (e.g. object storage).

**Status:** not defined. No media storage requirement identified yet — Gomrok mostly stores
records, not files, and provider-hosted checkout keeps card data off Gomrok entirely. Do not pick
a provider or record buckets/credentials here until there is a real need.

## To contain (if a need arises)

- Storage backend and why.
- Bucket / path layout, lifecycle rules.
- Access control, signed URLs.
- Backup / retention.
