# LocalAssets.md

**Purpose.** How locally-stored assets (uploads, generated files, fixtures) are handled in
development.

**Status:** none yet. The app stores no files. Update if/when a feature needs local asset storage
(and see `MediaStorage.md` for the production side).

## To contain (when relevant)

- Where local assets live on disk / in the container.
- What is git-ignored vs committed (fixtures).
- How they map to the production storage (`MediaStorage.md`).
